package org.example;

import utils.DB;

public class Main {
    public static void main(String[] args) {
        DB db = DB.getInstance();
        // Utilisez maintenant db pour accéder à la connexion à la base de données
    }
}
